on: push
name: Transfer Files
jobs:
  FTP-Deploy-Action:
    name: FTP-Deploy-Action
    runs-on: ubuntu-latest
    steps:
      - name: Install SSH Key
        uses: shimataro/ssh-key-action@v2
        with:
         SSH_PRIVATE_KEY: SECRETS 
         SOURCE: "./"
         REMOTE_HOST: 165.232.184.154 
         REMOTE_USER: cicddatanethosti 
         TARGET: /home/cicddatanethosti
